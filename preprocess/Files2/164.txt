given coordin four point plane find four point form squar 
check squar need check follow 
four side form point 
b angl two side 90 degre 
condit requir quadrilater also side 
strongli recommend minim browser tri first 
idea pick point calcul distanc rest point 
let pick pick point p 
form squar distanc two point must p let distanc distanc one point must differ must equal 2 time let point differ distanc q 
condit good enough point differ distanc side 
also need check q distanc 2 point distanc c implement idea 
c program check four given point form squar 
includ iostream use namespac std structur point 2d space struct point int x util function find squar distanc point p poitn q int distsq point p point q return p x q x p x q x p q p q function return true p1 p2 p3 p4 form squar otherwis fals bool issquar point p1 point p2 point p3 point p4 int d2 distsq p1 p2 p1 p2 int d3 distsq p1 p3 p1 p3 int d4 distsq p1 p4 p1 p4 length p1 p2 p1 p3 follow condit must met form squar 
1 squar length p1 p4 twice squar p1 p2 2 p4 distanc p2 p3 d2 d3 2 d2 d4 int distsq p2 p4 return distsq p3 p4 d2 two case similar case d3 d4 2 d3 d2 int distsq p2 p3 return distsq p2 p4 d3 d2 d4 2 d2 d3 int distsq p2 p3 return distsq p3 p4 d2 return fals driver program test function int main point p1 20 10 p2 10 20 p3 20 20 p4 10 10 issquar p1 p2 p3 p4 
cout ye cout return 0 output ye articl contribut anuj 
pleas write comment find anyth incorrect want share inform topic discuss adsbygoogl window adsbygoogl push 
